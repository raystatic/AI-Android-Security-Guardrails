<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <permission
        android:name="com.example.rulesreviewer.permission.ACCESS_TOKEN"
        android:protectionLevel="signature" />

    <permission
        android:name="com.example.rulesreviewer.permission.SYNC_SIGNAL"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RulesReviewer"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.RulesReviewer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:label="Deep Link">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="myapp" />
            </intent-filter>
        </activity>

        <service
            android:name=".AppSyncService"
            android:exported="false" />

        <receiver
            android:name=".AppBroadcastReceiver"
            android:exported="true"
            android:permission="com.example.rulesreviewer.permission.SYNC_SIGNAL">
            <intent-filter>
                <action android:name="com.example.rulesreviewer.ACTION_SYNC" />
            </intent-filter>
        </receiver>

        <provider
            android:name=".TokenProvider"
            android:authorities="com.example.rulesreviewer.tokens"
            android:exported="true"
            android:permission="com.example.rulesreviewer.permission.ACCESS_TOKEN" />

        <activity
            android:name=".WebViewActivity"
            android:exported="false" />

        <activity
            android:name=".PaymentActivity"
            android:exported="false" />

        <activity
            android:name=".SampleSecurityIssueActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="http" android:host="insecure.com" />
            </intent-filter>
        </activity>
    </application>

</manifest>